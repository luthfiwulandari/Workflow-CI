name: Heart-Disease-Prediction

# Define the entry point for the project
entry_points:
  main:
    # Command to run the modelling script
    # Make sure your modelling.py script is compatible with being run directly
    # and reading data from its current directory if needed.
    command: "python modelling.py" # Pastikan baris ini ada dan benar

    # Specify the environment to use (conda)
    # This refers to the conda.yaml file
    parameters:
      data_path: {type: string, default: "heart_clean.csv"}
